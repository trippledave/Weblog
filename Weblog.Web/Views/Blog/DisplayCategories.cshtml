@model List<Weblog.Web.Models.Weblog.CategoryModel>
@if (Model.Count > 0)
{
    <table class="table">
        <tr>
            <th class="first">ID</th>
            <th>Name</th>
            <th>Löschen</th>
        </tr>

        @foreach (var item in Model)
        {
                        String idString = "row" + item.ID;
                        
            <tr id=@idString>
                <td>@item.ID</td>
                <td>@item.Name</td>
                <td>
                    @using (Ajax.BeginForm(new AjaxOptions() { Url = "/Blog/DeleteCategory/" + @item.ID, OnSuccess = "SiteHelpers.test(" + @item.ID + ")" }))
                    {
                        @Html.AntiForgeryToken()
                        <button>Löschen</button>
                    }


                </td>
            </tr>
        }
    </table>
}